- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.scripts: scripts
    ~/.config/hypr: hypr
    ~/.config/waybar: waybar
    ~/.config/swaync: swaync
    ~/.profile: home/.profile
    ~/.zshrc: home/.zshrc
